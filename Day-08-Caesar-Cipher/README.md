# Day 8 - Caesar Cipher 🔒✉️

A fully functional Caesar Cipher program that can **encrypt** and **decrypt** messages using a shift key.

Users choose:
- encode (encrypt) or decode (decrypt)
- Enter the message
- Enter the shift number

The program handles letters only (a-z), wraps around the alphabet (z → a), and preserves case? Wait — Angela's version usually keeps it simple lowercase, but yours might handle uppercase/symbols.

## What I Learned
- Functions with inputs and parameters
- Default parameters
- Loops with range and string manipulation
- Alphabet list and indexing
- Combining everything into a reusable cipher function

## How to Run
Run `main.py` (or `caesar_cipher.py`)  
→ Type 'encode' or 'decode'  
→ Enter message and shift  
→ Get the result!  
Type 'yes' to go again.

## Example
Text: "hello"  
Shift: 5  
Encode → "mjqqt"  
Decode → back to "hello"

Demo on X: https://x.com/Devang_Sankhla/status/2002688546403135873?s=20

One of the coolest projects so far — feels like real cryptography!
